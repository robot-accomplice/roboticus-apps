# Config fields merged into the profile's roboticus.toml during install.
# Only fields listed here are overridden; all other config is preserved.

[agent]
delegation_enabled = true
composition_policy = "propose"
skill_creation_rigor = "full"
output_validation_policy = "strict"

[models.routing]
mode = "primary"
